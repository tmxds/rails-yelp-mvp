<h1>Leave a review for <%= @restaurant.name %></h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :rating, collection: 1..5 %>
  <%= f.input :content %>
  <%= f.hidden_field :restaurant %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
